# ICP 查询系统 - 稳定性修复记录

> 记录 2026-09-04 的稳定性修复：解决"长任务把前缀烧穿后系统瘫痪"的问题。
> 目标：健康时全速（实测 60~119 q/s），被风控时自动退避且不自我加剧。

## 一、问题现象（实测复现）

同一前缀 `2409:8a1a:1234:4ab0`、同一份 8/22 恢复版代码：

| 时刻 | 操作 | 结果 |
|---|---|---|
| 23:27 | 2000 条短任务 | 119 q/s，100% 成功 |
| 23:30 | 6082 条任务（32 worker） | 掉到 30% 成功，网络错 4215 |
| 23:36 | 之后小任务 | 1.6 q/s，auth 90% 被 403 拦 |

**根因链路**：
1. 长任务持续满速 → 前缀被 WAF 盯上，单 IP 逐个硬化；
2. 硬化 IP 触发"死 IP 替换"（每 2 秒 netsh 删/加一个新 IP）；
3. 新 IP 立即被查询、立即被拦 → 替换风暴无限循环，反而把整个 /64 前缀的
   auth 请求量堆到 WAF 极限；
4. WAF 升级为 auth 直接 403（取号全拦）→ 系统瘫痪（0.7~3 q/s）。

## 二、修复内容（2026-09-04）

### 1. 前缀熔断 = 指数退避（原固定 20~30 秒太短）

触发条件（更早发现风控）：
- 连续打码失败 ≥ 5 次（原来是 12，等烧穿才触发）；
- 20 秒窗口失败率 > 45%（原来是 60%）。

冷却时长指数递增：`60s → 120s → 240s → 480s`（封顶 8 分钟），
按前缀独立（互不拖累）。auth 恢复正常（任意打码成功）后级别归零。

熔断期间所有 worker 暂停取号/查询/打码，让 WAF 计数真正衰减，
而不是"熔断 20 秒 → 恢复满速 → 再熔断"的空转。

### 2. 死 IP 替换闸门（关键：替换不再自我加剧）

`_replace_blocked_ip`（netsh 删旧 IP + 补新 IP）现在受闸门控制：
- 前缀被整体风控（熔断触发）时，**停止所有死 IP 替换**；
- auth 恢复（有打码成功）后闸门才重新开放。

原因：前缀被烧时，新补的 IP 也会被立即拦，替换风暴只是徒增请求、
加剧风控。实测 6082 任务中替换达每 2 秒一次，是烧穿前缀的放大器。

### 3. 查询身份一致性修复（2026-09-05）

**问题**：取号+打码得到的 token/uuid/sign 是在"打码会话 cookie"身份下签发的；
但查询前代码把请求头换成了该 IP 的 fingerprint（另一套自造 UA/`__jsluid_s` cookie），
导致"打码身份 A + 查询身份 B"割裂，WAF 判异常 → 频控 403。

**修复**：查询一律沿用打码返回的完整 headers（`current_headers = hd`），
换 IP 复用 token 时也保持同一套 cookie，保证 auth→打码→查询 同一会话连贯。

**实测**：单 IP 连续查询，cookie 一致后前 20 条 13/20；20 条后仍硬化（0/20），
说明当前环境单 IP 硬化阈值约 20 条，且整体受前缀状态限制。

## 二（续）、查询方式修复（2026-09-05 中午，决定性）

### 1. 连接池容量修复（最决定性）

**问题**：32 worker × 3 并发 = 96 同时查询，但 aiohttp 连接池
`limit_per_host=50` → 46 个请求排队等连接，keep-alive 占 30s 不放 →
5s 超时 → **网络错洪峰**（5000 条任务 2946~4569 个网络错，直接瘫痪）。

**修复**：`limit_per_host: 50 → 160`、`keepalive_timeout: 30 → 8`。
网络错从 2946 降到 155（降 95%）。

### 2. 单凭证配额（credential_query_cap）

**问题**：一个 sign（token/uuid/cookie）在 300s 内可无限跨 IP 复用，
被 WAF 识别"同一把钥匙高频开锁" → 频控。

**修复**：每个 sign 服务 `credential_query_cap`（现 20）条后强制换新 sign
（重新打码 = 新身份 + 新会话）。403 从瘫痪级降到 3~7%。

### 3. 429 / 403-streak 冷却 300s → 60s

429（访问频次过高，IP 级）和持续 403 都是瞬时硬化，60s 后 IP 即可重用。
300s 会把 300 个 IP 的小池全部锁死 → 无 IP 可用 → 网络错。

### 4. 会话一致性（上一节 3 已述）：查询沿用打码 headers(cookie)。

## 三、物理边界（实测确认，非代码可绕过）

**取号（auth）接口有独立时间窗限流**：

| 任务间隔 | auth 被拦 | 结果 |
|---|---|---|
| 单次(休息≥10分钟) | 0 | 90~96% 成功，50 q/s |
| 间隔 2~3 分钟 | 136~212 | 崩(8~29%) |

单次任务内打码 2.6 次/秒安全；跨任务间隔 <5~8 分钟会累积触发取号限流。
真实场景"一次提交大任务"不受影响；连续压测需间隔 10 分钟以上。

## 四、当前正式配置（config.yml）

```text
batch_workers           = 32
ip_query_concurrency    = 3
ip_queries_per_rotation = 5
queries_per_ip          = 20
credential_query_cap    = 20
pool_num                = 300
连接池                  = limit 400 / per_host 160 / keepalive 8
```

### 3. 保留已验证的高速逻辑（8/22 恢复版）

- token 跨 IP 复用（300 秒生命周期内换 IP 不重新打码）；
- 单 sign 连续失败 → 放弃换新 sign（实测提速 2.5 倍）；
- 每 IP 查询硬化曲线适配：前 20 条 85%、40 条 65%、60 条 55%；
- 组间随机延迟 0.05~0.25s + worker 启动相位打散；
- 死 IP 启动预检过滤 + 尾部完成事件（消除收尾空等）。

## 三、关键实测依据

### 单 IP 连续查询硬化曲线（3 个干净 IP 重复验证）

| 查询段 | 平均成功率 |
|---|---|
| 1~20 条 | 85% |
| 21~40 条 | 65% |
| 41~60 条 | 55% |
| 61~80 条 | 30% |
| 81~100 条 | 15% |

→ 结论：每 IP 查 10~20 条即轮换是最优区间（配置 `ip_queries_per_rotation: 10`）。

### 404/429/风控信号

官网返回三种"打扰"信号，全部按前缀聚合速率触发：
- 查询 403 挑战页：`error_403_type=frequency_high`（"您访问频率太高"）；
- auth 403：`当前访问已被创宇盾拦截`；
- 业务 429：`访问频次过高`（HTTP 200 + code 429）。

## 四、当前正式配置（config.yml）

```text
batch_workers           = 32
ip_query_concurrency    = 3
ip_queries_per_rotation = 10
queries_per_ip          = 20
pool_num                = 300
```

## 五、验证记录

| 时间 | 场景 | 结果 |
|---|---|---|
| 2026-09-04 23:27 | 2000 条（恢复版） | 119 q/s，100% |
| 2026-09-04 23:41 | 300 条（修复后） | 44 q/s，100%（前缀恢复中） |

> 注意：当前家宽前缀 2026-09-04 已被运营商从 `1231:b90` 重新分配为
> `1234:4ab0`。新前缀需要"养熟"，被 WAF 盯死时应静置恢复，不要连续测压。

## 六、回滚

- 修复前的当前版本备份：`ymicp.py.reconstructed-20260904.bak`（8/28 重构版）
- 8/22 验证版备份：`ymicp.py.before-restore-20260822.bak`
